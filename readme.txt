To read a scene, change INPUT_FILE variable in game.ccp line 20 from scene to what you need,


you can change the trace depth, the picture size in (default is 512 x 512) and disable the multi_sampling by changing PICTURE_SIZE, DEPTH and DO_MULTI_SMAPLING
